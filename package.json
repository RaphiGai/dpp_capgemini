{
  "name": "BTP_TEST",
  "version": "1.0.0",
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": "^9",
    "@sap/xssec": "^4.13.0"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "start": "cds-serve",
    "build": "cds build --production && node -e \"require('fs').cpSync('app','gen/srv/app',{recursive:true})\""
  },
  "private": true,
  "cds": {
    "requires": {
      "auth": {
        "[development]": "dummy",
        "[production]": "xsuaa"
      }
    }
  }
}
